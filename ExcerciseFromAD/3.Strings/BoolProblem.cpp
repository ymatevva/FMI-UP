#include <iostream>
bool isTrue(int number)
{
    int lastLastDigit = (number/10)%10;
    if (lastLastDigit==0||number%lastLastDigit!=0)
    {
        return true;
    }
    return false;
}

bool isInPainted(double x,double y)
{
    if (x>=-2 && x<=1 && )
}

int main()
{
    int num;
    std :: cin >> num;

    double x,y;
    std :: cin >> x >> y;








    return 0;
}